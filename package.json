{
  "name": "contracts-test",
  "version": "1.0.0",
  "description": "BCCA Contracts Assistant",
  "main": "server.js",
  "scripts": {
    "postinstall": "cd my-chat-ui && npm install && npm run build && cd .. && cp -r my-chat-ui/build ./build",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "openai": "^4.38.2"
  }
}
